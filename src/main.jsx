import React from "react";
import ReactDOM from "react-dom/client";
import SmileStreak from "./SmileStreak";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SmileStreak />
  </React.StrictMode>
);
